from setuptools import setup, find_packages

setup(
    name="fatima-utils-here",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[],
    long_description=open("README.md").read(),
    long_description_content_type="text/markdown",
)